version: '3'
services:
  serve-server:
    image: nginx:1.13.10
    restart: always
    ports:
      - 8090:8090
    volumes:
      - /var/data/webdav:/var/www
    environment:
      SERVICE_NAME: system/webdav
      SERVICE_CHECK_TCP: "true"
